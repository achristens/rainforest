<ul>
  <% @products.each do |product| %>
    <li>
      <p><%= product.name %></p>
      <p><%= product.description %></p>
      <p>$<%= product.price_in_cents %></p>
      <p><%= link_to "View Product", "/products/#{product.id}" %></p>
    </li>
  <% end %>
</ul>
